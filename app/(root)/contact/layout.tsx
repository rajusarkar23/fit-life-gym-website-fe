export const metadata = {
  title: "Contact - FitLife Gym",
  decrition: "Contact page of FitLife Gym",
};
export default function ContantLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return <div>{children}</div>;
}
