export const metadata = {
    title: "Verify - FitLife Gym",
    description: "Verify page of FitLife Gym",
  };
  
  export default function VerifyLayout({
    children,
  }: Readonly<{ children: React.ReactNode }>) {
    return <div>{children}</div>;
  }
  