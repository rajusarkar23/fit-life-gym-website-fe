export const metadata = {
  title: "Membership - FitLife Gym",
  description: "Membership page of FitLife Gym",
};

export default function MembershipLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return <div>{children}</div>;
}
