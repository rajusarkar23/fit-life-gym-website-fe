export const metadata = {
    title: "Admin otp verify - FitLife Gym",
    description: "Admin otp verify page of FitLife Gym",
  };
  
  export default function VerifyLayout({
    children,
  }: Readonly<{ children: React.ReactNode }>) {
    return <div>{children}</div>;
  }
  