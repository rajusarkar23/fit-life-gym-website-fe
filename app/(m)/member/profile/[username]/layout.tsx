export const metadata = {
    title: "Member Profile - FitLife Gym",
    description: "Member Profile page of FitLife Gym"
}
export default function MemberProfileLayout({children}: Readonly<{children: React.ReactNode}>){
    return <div>{children}</div>
}