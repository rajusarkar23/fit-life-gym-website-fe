export const metadata = {
    title: "Member Space - FitLife Gym",
    description: "Member Space page of FitLife Gym"
}
export default function MemberSpaceLayout({children}: Readonly<{children: React.ReactNode}>){
    return <div>{children}</div>
}